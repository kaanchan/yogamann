# ── LEGACY FALLBACK ─────────────────────────────────────────────────────────
# The canonical package spec is now pyproject.toml (uv-managed).
# Use this file only if uv is unavailable:
#
#   pip install -r requirements.txt
#
# Preferred setup:
#   uv sync          (reads pyproject.toml, installs torch from nightly cu130)
# ─────────────────────────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cu128

torch>=2.6.0
torchvision

diffusers>=0.29.0
transformers>=4.41
safetensors>=0.3.1
huggingface-hub>=0.33
accelerate

controlnet_aux>=0.0.8
onnxruntime-gpu

opencv-python
pillow
timm
numpy
pyyaml
